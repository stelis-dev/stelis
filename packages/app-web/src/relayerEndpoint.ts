import { APP_WEB_RELAYER_BASE } from './runtimeEnv';

/**
 * Shared relayer base URL for all components.
 * Always explicit: no runtime fallback.
 */
export const RELAYER_BASE = APP_WEB_RELAYER_BASE;
